#!/bin/sh
# Mock git 2.37 — below merge-tree --write-tree cutoff (2.38).
if [ "$1" = "--version" ]; then
    echo "git version 2.37.4"
    exit 0
fi
exec /usr/bin/env -u PATH PATH="$MOCK_REAL_PATH" git "$@"
